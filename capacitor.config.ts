import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ui-workshop',
  webDir: 'dist/ui-workshop/browser',
};

export default config;
